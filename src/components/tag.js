import React from 'react'

const Tag = (props) => {
    return <div style={{border: '1px solid grey', padding: '5px 10px', borderRadius: '30px', display:'inline-block'}} >{props.singletag}</div>;
  }

export default Tag
